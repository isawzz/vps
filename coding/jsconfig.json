{
  "compilerOptions": {
		"target": "ESNext",
    // This must be specified if "paths" is set
    "baseUrl": ".",
    // Relative to "baseUrl"
    "paths": {
			"~/*": ["./src/*"]
		}
  },
	"exclude": ["cb2"]
}